# Visual flow

The frontend is an execution-flow explorer, not an arbitrary workflow builder. Presets configure safe playground inputs. XYFlow renders known stages and provider paths, while the trace returned by FastAPI determines status, timing, badges, skipped nodes, and active edges. Instant mode shows the completed result; replay is a lightweight frontend presentation of completed spans. The inspector exposes span metadata without credentials.
